$(document).ready(function() {
  // Máscara para telefone
  $('#telefone').mask('(00) 0000-0000');
  
  // Máscara para CPF
  $('#cpf').mask('000.000.000-00');
  
  // Máscara para CEP
  $('#cep').mask('00000-000');
});
